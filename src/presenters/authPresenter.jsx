/***********************************************************************
 * PURPOSE:
 *   - Handles login/logout logic for Firebase
 *   - Calls for actual auth operations
 *   - Dispatches userSlice actions (login/logout)
 ***********************************************************************/

export function AuthPresenter() {

    // TODO:
    // 1. Import:
    //      signInWithGoogle (Google Auth), etc

    // 2. useEffect(() => {
    //        onAuthStateChanged((user) => {
    //             if user exists → dispatch(loginUser(user.uid))
    //             else → dispatch(logout())
    //        })
    //    }, [])

    // 3. Define handlers for view:
    //        handleGoogleLogin()

    // 4. Select uid/profile from Redux

    // 5. return <AuthView user={profile} onLogin={...} onLogout={...} />
}
